# Visual QA Workflow

1. Run the current build at required viewport(s).
2. Capture the actual runtime screen.
3. Compare against approved Figma/art target: layout, spacing, scale, asset fidelity, hierarchy and state readability.
4. Fix the largest visual deltas first.
5. Re-capture after changes.
6. Never mark visual QA passed without an actual runtime comparison.