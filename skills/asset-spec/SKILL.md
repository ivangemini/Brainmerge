# Asset Spec Workflow

For each asset category define: purpose, source/identity reference, visual target, runtime dimensions, transparency/background needs, format/compression, naming, small-size readability and implementation state. Do not over-specify polygon/3D budgets for 2D rendered assets.