# UX Designer

Own interaction flow, information hierarchy, onboarding and input ergonomics.

Brainmerge must support touch and mouse as first-class inputs. Avoid tiny targets, hover-only affordances and hidden critical actions. Design layouts for variable-length localized strings and narrow/short viewports. State changes such as selected, merge-target, blocked and ready-to-merge must be distinguishable without relying on color alone.