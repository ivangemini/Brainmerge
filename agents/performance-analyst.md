# Performance Analyst

Own measured browser performance, load weight, memory and interaction latency.

Priorities:
- fast initial load on mobile networks;
- smooth drag/merge interaction on mobile-class hardware;
- bounded asset memory;
- avoid layout thrashing and unnecessary per-frame allocations in proven hot paths;
- profile before/after meaningful optimization work;
- set concrete budgets once the runtime stack and first playable build exist.